// Demo-only: a single hardcoded user profile. A real implementation would
// read this from the authenticated user's account.
export const mockUser = {
  name: "Alex",
  allergies: ["shellfish"],
};
